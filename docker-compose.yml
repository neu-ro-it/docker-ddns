version: '3'
services:
  ddns:
    image: neu-ro-it/docker-ddns:latest
    #image: neu-ro-it/docker-ddns:develop
    #build: .
    restart: always
    environment:
      RECORD_TTL: 60
      ZONE: example.org
      SHARED_SECRET: changeme
    ports:
      - "53:53"
      - "53:53/udp"
      - "8080:8080"
#    volumes:
#       - ./bind-data:/var/cache/bind
